# A program that does nothing. See startup.sl.
#
# Ruby's start-up loads RubyGems unless told not to, and `run.sh` does not tell it: a program a
# person runs with `ruby` pays for RubyGems, so the figure includes it.
