<?php
// A program that does nothing. See startup.sl.
//
// `php` loads every extension its ini file names before it reads a line of this, and a program a
// person runs with `php` pays for that, so the figure includes it.
